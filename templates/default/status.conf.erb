server {
  listen 80 default;
  server_name "";

  location /nginx_status {
    stub_status on;
    access_log  off;
    allow       <%= node[:nginx][:status][:allow] %>;
    deny        <%= node[:nginx][:status][:deny] %>;
  }
}
